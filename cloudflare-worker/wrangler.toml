name = "pos-toggle"
main = "src/worker.js"
compatibility_date = "2024-09-23"

# Nightly re-offline cron: midnight AEST = 2 PM UTC
[[kv_namespaces]]
binding = "AUDIT_LOG"
id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID"

[triggers]
crons = ["0 14 * * *"]
